#ifndef _LOGGER_H_
#define _LOGGER_H_

/*
    struct msg m;
    m.size - размер строки
    m.data - строка
*/

/* После каждого сообщения производится перевод строки,
 * если этого не сделано в сообщении. */

#endif
