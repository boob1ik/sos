//#include "rbuf.h"
//#include "mem\kmem.h"
////#include "type.h"
////#include "lib\mylibc.h"
//#include <stddef.h>
//
//struct rbuf* rbuf_create (int size, int pid)
//{
////    struct rbuf *rb = malloc(sizeof(struct rbuf));
////    if (!rb)
////        return NULL;
////
////    rb->size = size;
////    //rb->buf = malloc(size, pid);
////    if (!rb->buf) {
////        free(rb);
////        return NULL;
////    }
////    rb->cnt = rb->head = rb->tail = 0;
////    return rb;
//    return NULL;
//}
//
//void rbuf_delete (struct rbuf* rb)
//{
////    free(rb->buf);
////    free(rb);
//}
